import { component$ } from '@builder.io/qwik';
import { PlatformTenantsPage } from '~/components/platform/platform_tenants_page';

export default component$(() => {
  return <PlatformTenantsPage />;
});
